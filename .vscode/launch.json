{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "debug node port 9229",
            "remoteRoot": "/var/www/tanukicode-ui", // Путь внутри контейнера
            "localRoot": "${workspaceFolder}/public", // Путь на вашей локальной машине
            "port": 9229,
            "address": "localhost",
        }
    ]
}